[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "minitorch-module-1-jducr17"
version = "0.1.0"
description = "MiniTorch Module 1"
readme = "README.md"
requires-python = ">=3.10,<3.11"
dependencies = []

[tool.setuptools.packages.find]
where = ["."]
include = ["minitorch*"]
